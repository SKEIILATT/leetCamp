import { Component } from '@angular/core';

/**
 * Placeholder técnico — nunca se renderiza de verdad. `landingGuard` siempre
 * redirige antes de que este componente llegue a activarse; existe solo
 * porque una ruta necesita un componente.
 */
@Component({
  selector: 'app-landing',
  template: '',
})
export class Landing {}
